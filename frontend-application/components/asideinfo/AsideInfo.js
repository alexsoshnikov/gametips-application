import classes from "../../styles/components/AsideInfo.module.scss";

export const AsideInfo = () => {
    return (
        <aside className={"position-sticky " + classes.info}>
            Information
        </aside>
    )
}